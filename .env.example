# Database (PostgreSQL)
# Used by: pipeline, reporting, local runs. For Docker, also set POSTGRES_* for docker-compose.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=MyDB
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=MyDB

# FastAPI fake data API
# Used by: pipeline when calling the API
API_HOST=localhost
API_PORT=8000

# Optional: full DB URL for reporting (if not set, built from DB_* above)
# DB_URL=postgresql://postgres:your_password@localhost:5432/MyDB

# Optional: disable wait for services (set to 0 when DB and API are already up)
# WAIT_FOR_SERVICES=1

# Optional: path to public CSV (default: data/DailyDelhiClimateTrain.csv)
# PUBLIC_DATA_PATH=data/DailyDelhiClimateTrain.csv
